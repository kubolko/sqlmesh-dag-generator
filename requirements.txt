# Tested with sqlmesh 0.228.x (prod image) through 0.236.x (latest at 0.9.11)
sqlmesh>=0.228.0
apache-airflow>=2.4.0
pyyaml>=5.4.0
# Used for mixed-cadence skip_if_not_due (also pulled in by Airflow)
croniter>=1.0.0


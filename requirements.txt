# Runtime dependencies. pyproject.toml is the source of truth for the package;
# this file exists for Docker images and `pip install -r requirements.txt` setups.
sqlmesh>=0.228.0
apache-airflow>=2.4.0
pyyaml>=5.4.0
croniter>=1.0.0
